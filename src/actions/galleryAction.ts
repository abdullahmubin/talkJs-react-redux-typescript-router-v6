import { GalleryActionType } from "../action-types/galleryActionTypes"

// NOTE: according to requirement, we don't need it.. we need just reducer and store. '
interface Addallery {
    type: "Addallery",
    payload: {
        
    }
}

export type GalleryAction  = Addallery